# GitHub skill knowledge

- Prefer fine-scoped PATs or GitHub Apps over classic tokens.
- Never commit secrets; use GitHub Actions secrets.
- PRs are the review boundary (GT standard).
- CI workflow files belong under `.github/workflows/`.

# /projects

One folder per client engagement. Each project workspace contains its PRD,
architecture, decision records (mirrored to `/decisions`), stage artifacts,
gate scores, metrics, and — before closure — its learning report (02 §8).

A project is not closed until its learning report is merged into
`/knowledge`.
